kind: ImageStream
apiVersion: image.openshift.io/v1
metadata:
  name: geoserver-cloud-rest
  labels:
    app: geoserver-cloud-rest
    app.kubernetes.io/component: geoserver-cloud-rest
    app.kubernetes.io/instance: geoserver-cloud-rest
spec:
  lookupPolicy:
    local: false
  tags:
    - name: 1.5.0
      annotations:
        openshift.io/imported-from: 'geoservercloud/geoserver-cloud-rest:1.5.0'
      from:
        kind: DockerImage
        name: 'geoservercloud/geoserver-cloud-rest:1.5.0'
      generation: 2
      importPolicy:
        importMode: Legacy
      referencePolicy:
        type: Source
