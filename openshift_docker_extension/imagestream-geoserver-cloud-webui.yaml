kind: ImageStream
apiVersion: image.openshift.io/v1
metadata:
  name: geoserver-cloud-webui
  labels:
    app: geoserver-cloud-webui
    app.kubernetes.io/component: geoserver-cloud-webui
    app.kubernetes.io/instance: geoserver-cloud-webui
spec:
  lookupPolicy:
    local: false
  tags:
    - name: 1.5.0
      annotations:
        openshift.io/imported-from: 'geoservercloud/geoserver-cloud-webui:1.5.0'
      from:
        kind: DockerImage
        name: 'geoservercloud/geoserver-cloud-webui:1.5.0'
      generation: 2
      importPolicy:
        importMode: Legacy
      referencePolicy:
        type: Source
